import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  searchContainer: {
    alignItems: "center",
    flexDirection: "row",
    paddingTop: 8,
    paddingLeft: 8,
    paddingBottom: 8,
    borderColor: "#ccc",
  },
  searchTextInput: {
    marginLeft: 10,
    height: 30,
    width: "100%",
    paddingLeft: 8,
  },
});
export default styles;
